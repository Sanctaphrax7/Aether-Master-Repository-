@inject ISessionStorageService SessionStorage
@inject AuthStateProvider AuthState
@inject NavigationManager Nav



<AuthorizeView>
    <Authorized>
        <MudButton @onclick ="SignOut" Color="Color.Error" StartIcon="@Icons.Material.Filled.Logout">Sign Out</MudButton>
</Authorized>
</AuthorizeView>




@code {
    async Task SignOut()
    {

        var state = (AuthenticationStateProvider)AuthState;
        await AuthState.UpdateAuthenticationState(null);
        // await SessionStorage.RemoveItemAsync("token");
        // await AuthState.GetAuthenticationStateAsync();
        Nav.NavigateTo("/", true);
    }
}
